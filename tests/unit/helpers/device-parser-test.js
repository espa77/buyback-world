import { deviceParser } from 'buyback-world/helpers/device-parser';
import { module, test } from 'qunit';

module('Unit | Helper | device parser');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = deviceParser([42]);
  assert.ok(result);
});
