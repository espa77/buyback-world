<div class="device_listing">
    {{#each model as |device|}}
        <div class="device">
            <div class="device_title">
                <h3>{{device.name}}</h3>
            </div>
            <div class="device_images">
                <img src={{device.image}} class="img_device" />
            </div>
        </div>
    {{/each}}
</div>
