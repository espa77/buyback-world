<div class="device_listing">
  {{#each unique as |device|}}
    <div class="device">
    {{#paper-radio-group groupValue=groupValue onChange=(action 'changeGroupValue') as |group|}}
      {{#group.paper-radio }}
        <div class="device_images">
          <img src="assets/images/{{device.image}}" class="img_device" />
        </div>
      {{/group.paper-radio}}

    {{/paper-radio-group}}
      <div class="device_title">
        <h3>{{device.device_type}}</h3>
      </div>

    </div>
  {{/each}}

<div class="device-selector">
    {{#each unique as |device|}}
        {{device-selector device_type=device.device_type image=device.image}}
    {{/each}}
</div>