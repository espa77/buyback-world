{{#paper-radio-group groupValue=selectedDevice.device_attributes.size onChange=(action (mut selectedDevice.device_attributes.size)) as |group|}}
    {{#group.paper-radio value=size primary=true}}
        {{size}} GB
    {{/group.paper-radio}}
{{/paper-radio-group}}

