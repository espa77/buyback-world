{{#paper-radio-group groupValue=groupValue onChange=(action 'changeSizeValue') as |group|}}
    {{#group.paper-radio name="size" value=size}}
        {{size}} GB
    {{/group.paper-radio}}
{{/paper-radio-group}}
